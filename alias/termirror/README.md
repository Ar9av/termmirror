# termirror

This package exists so the one-`m` spelling finds its way home. The project is
[**termmirror**](https://www.npmjs.com/package/termmirror) — installing this installs that.

```bash
npm install termmirror
```
